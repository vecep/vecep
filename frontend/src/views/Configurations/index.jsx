import React from 'react';
import VecepButton from 'components/Button';

const Configurations = () => {
	return (
		<div>
			<VecepButton onClick={() => {}} variant="contained" color="error">
				Trocar senha
			</VecepButton>
		</div>
	);
};

export default Configurations;
